<header>
  <% if signed_in? %>
    <%= "Hello #{current_user.username}" %>
    <%= button_to "Sign Out!", session_url, method: :delete %>
  <% else %>
    <%= link_to "Sign Up!", new_user_url %>
    <%= link_to "Sign In!", new_session_url %>
  <% end %>
</header>
<h2>Stuff Goes Here! I promise!</h2>
<div id="content"></div>
